import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.calculadora',
  appName: 'Calculadora',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
